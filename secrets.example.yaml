# Copy to secrets.yaml (kept out of git) and fill in.
wifi_ssid: "4402IoT"
wifi_password: "FKwngtehCH3G"

# The API is unencrypted by default (LAN-only device). If you enable encryption
# in base/core.yaml, add the base64 key here too:
#   https://esphome.io/components/api.html#configuration-variables
# api_encryption_key: "PUT_YOUR_OWN_BASE64_KEY_HERE"
